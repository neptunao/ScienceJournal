<h1>Articles</h1>
<% if @articles.empty? %>
    There are no articles with this params.
<% else %>
    <ul>
        <% @articles.each do |article| %>
          <li>
            <%= link_to article.title, article %>
          </li>
        <% end %>
    </ul>
 <% end %>